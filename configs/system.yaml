# MonkAgents System Configuration

database:
  type: sqlite
  path: ./data/sqlite/monkagents.db

redis:
  host: localhost
  port: 6379
  db: 0

logging:
  level: info
  format: pretty

server:
  port: 3000
  host: localhost

agents:
  configPath: ./configs/agents