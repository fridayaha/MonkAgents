# MonkAgents System Configuration

database:
  type: mysql
  host: localhost
  port: 3306
  username: root
  password: root
  database: monkagents

redis:
  host: localhost
  port: 6379
  db: 0
  keyPrefix: 'monkagents:'

logging:
  level: info
  format: pretty

server:
  port: 3000
  host: localhost

agents:
  configPath: ./configs/agents